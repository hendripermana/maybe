<div class="space-y-6">
  <div>
    <h3 class="text-lg font-medium mb-2">Screen Reader Attributes</h3>
    <p class="mb-4">This example demonstrates proper ARIA attributes for screen reader accessibility:</p>
    <ul class="list-disc pl-5 mb-4">
      <li><code>aria-busy</code>: Indicates that content is loading</li>
      <li><code>aria-hidden</code>: Hides visual-only elements from screen readers</li>
      <li><code>role="status"</code>: Announces updates to screen readers</li>
      <li>Screen reader text: Provides context about loading state</li>
    </ul>
    
    <div class="p-4 border rounded-lg">
      <div aria-busy="true" role="status" class="space-y-4">
        <span class="sr-only">Loading content, please wait...</span>
        
        <%= render Ui::SkeletonComponent.new(width: "60%", height: "32px", aria: { hidden: "true" }) %>
        
        <div class="space-y-2">
          <%= render Ui::SkeletonComponent.new(width: "100%", height: "20px", aria: { hidden: "true" }) %>
          <%= render Ui::SkeletonComponent.new(width: "100%", height: "20px", aria: { hidden: "true" }) %>
          <%= render Ui::SkeletonComponent.new(width: "75%", height: "20px", aria: { hidden: "true" }) %>
        </div>
      </div>
      
      <div class="mt-4">
        <pre class="bg-muted p-2 rounded text-sm overflow-x-auto"><code>&lt;div aria-busy="true" role="status"&gt;
  &lt;span class="sr-only"&gt;Loading content, please wait...&lt;/span&gt;
  
  &lt;!-- Skeleton elements with aria-hidden="true" --&gt;
  &lt;div aria-hidden="true"&gt;...&lt;/div&gt;
&lt;/div&gt;</code></pre>
      </div>
    </div>
  </div>
  
  <div>
    <h3 class="text-lg font-medium mb-2">Loading States for Different Content Types</h3>
    <div class="space-y-4">
      <div aria-busy="true" role="status" class="border rounded-lg p-4 [data-theme=dark]:border-gray-700">
        <span class="sr-only">Loading user profile information...</span>
        
        <div class="flex items-center gap-4">
          <!-- Avatar placeholder -->
          <%= render Ui::SkeletonComponent.new(width: "64px", height: "64px", rounded: true, aria: { hidden: "true" }) %>
          
          <div class="space-y-2">
            <!-- Name placeholder -->
            <%= render Ui::SkeletonComponent.new(width: "150px", height: "24px", aria: { hidden: "true" }) %>
            
            <!-- Email placeholder -->
            <%= render Ui::SkeletonComponent.new(width: "200px", height: "16px", aria: { hidden: "true" }) %>
            
            <!-- Role placeholder -->
            <%= render Ui::SkeletonComponent.new(width: "100px", height: "16px", aria: { hidden: "true" }) %>
          </div>
        </div>
      </div>
      
      <div aria-busy="true" role="status" class="border rounded-lg p-4 [data-theme=dark]:border-gray-700">
        <span class="sr-only">Loading transaction data...</span>
        
        <div class="space-y-4">
          <% 3.times do %>
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <!-- Icon placeholder -->
                <%= render Ui::SkeletonComponent.new(width: "40px", height: "40px", rounded: true, aria: { hidden: "true" }) %>
                
                <div class="space-y-2">
                  <!-- Transaction name placeholder -->
                  <%= render Ui::SkeletonComponent.new(width: "180px", height: "20px", aria: { hidden: "true" }) %>
                  
                  <!-- Transaction date placeholder -->
                  <%= render Ui::SkeletonComponent.new(width: "120px", height: "16px", aria: { hidden: "true" }) %>
                </div>
              </div>
              
              <!-- Transaction amount placeholder -->
              <%= render Ui::SkeletonComponent.new(width: "100px", height: "24px", aria: { hidden: "true" }) %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  
  <div>
    <h3 class="text-lg font-medium mb-2">Dynamic Loading Announcements</h3>
    <p class="mb-4">This example demonstrates how to announce loading progress to screen readers:</p>
    
    <div class="p-4 border rounded-lg">
      <div aria-live="polite" role="status" class="space-y-4">
        <span class="sr-only">Loading account data, 3 of 5 accounts loaded...</span>
        
        <div class="flex items-center gap-2 mb-4">
          <%= render Ui::SkeletonComponent.new(width: "24px", height: "24px", class: "rounded-full", aria: { hidden: "true" }) %>
          <span>Loading accounts...</span>
        </div>
        
        <div class="space-y-4">
          <% 3.times do %>
            <div class="border rounded-lg p-3 [data-theme=dark]:border-gray-700">
              <div class="flex items-center justify-between">
                <div class="flex items-center gap-3">
                  <%= render Ui::SkeletonComponent.new(width: "32px", height: "32px", rounded: true, aria: { hidden: "true" }) %>
                  <%= render Ui::SkeletonComponent.new(width: "120px", height: "20px", aria: { hidden: "true" }) %>
                </div>
                <%= render Ui::SkeletonComponent.new(width: "80px", height: "24px", aria: { hidden: "true" }) %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
      
      <div class="mt-4">
        <pre class="bg-muted p-2 rounded text-sm overflow-x-auto"><code>&lt;div aria-live="polite" role="status"&gt;
  &lt;span class="sr-only"&gt;Loading account data, 3 of 5 accounts loaded...&lt;/span&gt;
  
  &lt;!-- Loading indicator and skeleton content --&gt;
&lt;/div&gt;</code></pre>
      </div>
    </div>
  </div>
</div>