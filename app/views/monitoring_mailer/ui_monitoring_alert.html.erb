<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  <style>
    .alert-box {
      padding: 15px;
      margin-bottom: 20px;
      border: 1px solid transparent;
      border-radius: 4px;
    }
    .alert-error {
      color: #721c24;
      background-color: #f8d7da;
      border-color: #f5c6cb;
    }
    .alert-warning {
      color: #856404;
      background-color: #fff3cd;
      border-color: #ffeeba;
    }
    .alert-info {
      color: #0c5460;
      background-color: #d1ecf1;
      border-color: #bee5eb;
    }
    .details {
      background-color: #f8f9fa;
      padding: 10px;
      border-radius: 4px;
      margin-top: 15px;
      white-space: pre-wrap;
      font-family: monospace;
    }
  </style>
</head>
<body>
  <h1>UI Monitoring Alert: <%= @title %></h1>
  
  <div class="alert-box alert-<%= @severity %>">
    <p><strong>Category:</strong> <%= @category.to_s.humanize %></p>
    <p><strong>Severity:</strong> <%= @severity.to_s.humanize %></p>
    <p><strong>Time:</strong> <%= @timestamp.strftime("%Y-%m-%d %H:%M:%S %Z") %></p>
  </div>
  
  <h2>Alert Details</h2>
  <div class="details">
    <%= @message %>
  </div>
  
  <p>
    <a href="<%= monitoring_url(host: ENV.fetch('APPLICATION_HOST', 'localhost:3000')) %>">
      View Monitoring Dashboard
    </a>
  </p>
  
  <hr>
  <p>
    This is an automated message from the Maybe Finance UI Monitoring System.
    Please do not reply to this email.
  </p>
</body>
</html>